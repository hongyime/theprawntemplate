# Project State

Current task: none.

Last meaningful change: repository created from `hongyime/theprawntemplate`.

Next step: replace this template with real project state when an active task
needs cross-agent handoff.

Notes:

- Do not store secrets or personal details here.
- Use `.agents/handoffs/` for longer handoffs.
